# Updated Template with Direct Push Approach

```yaml
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: eks-cluster-crossplane
  title: Kubernetes Cluster with Crossplane
  description: Create a new Kubernetes cluster using Crossplane
  tags:
    - kubernetes
    - crossplane
    - infrastructure
spec:
  owner: platform-team
  type: infrastructure
  parameters:
    - title: Cluster Configuration
      required:
        - name
        - environment
        - clusterType
      properties:
        name:
          title: Cluster Name
          type: string
          pattern: '^[a-z0-9-]+$'
          description: Name for your cluster (lowercase, numbers, hyphens only)
        environment:
          title: Environment
          type: string
          enum:
            - dev
            - staging
            - prod
          description: Target environment for the cluster
        clusterType:
          title: Cluster Type
          type: string
          enum:
            - standard
            - gpu-enabled
            - cost-optimized
            - high-availability
          description: Type of cluster configuration to deploy

    - title: Optional Settings
      properties:
        region:
          title: AWS Region
          type: string
          default: us-east-2
        nodeCount:
          title: Node Count
          type: integer
          default: 1
          minimum: 1
          maximum: 10
        nodeSize:
          title: Node Instance Type
          type: string
          default: t3.medium
          enum:
            - t3.medium

  steps:
    - id: fetch-cluster-template
      name: Fetch Cluster Template
      action: fetch:template
      input:
        url: ./templates/${{ parameters.clusterType }}
        values:
          name: ${{ parameters.name }}
          environment: ${{ parameters.environment }}
          clusterType: ${{ parameters.clusterType }}
          region: ${{ parameters.region }}
          nodeCount: ${{ parameters.nodeCount }}
          nodeSize: ${{ parameters.nodeSize }}

    - id: publish-pr
      name: Create Pull Request
      action: publish:github:pull-request
      input:
        repoUrl: github.com?owner=SkylarHoughtonGithub&repo=skylab-backstage-configs
        branchName: add-${{ parameters.environment }}-${{ parameters.clusterType }}-${{ parameters.name }}
        title: 'Add ${{ parameters.clusterType }} cluster: ${{ parameters.name }}'
        description: |
          ## New Cluster Configuration
          
          - **Cluster Name**: ${{ parameters.name }}
          - **Environment**: ${{ parameters.environment }}
          - **Type**: ${{ parameters.clusterType }}
          - **Region**: ${{ parameters.region }}
          - **Node Count**: ${{ parameters.nodeCount }}
          - **Node Size**: ${{ parameters.nodeSize }}
          
          This PR adds a new cluster configuration to `${{ parameters.environment }}/${{ parameters.clusterType }}/${{ parameters.name }}/`
        targetPath: ${{ parameters.environment }}/${{ parameters.clusterType }}/${{ parameters.name }}

    - id: wait-for-merge
      name: Wait for PR to be merged
      action: debug:wait
      input:
        seconds: 300

    - id: register
      name: Register in Catalog
      action: catalog:register
      input:
        catalogInfoUrl: https://github.com/SkylarHoughtonGithub/skylab-backstage-configs/blob/main/${{ parameters.environment }}/${{ parameters.clusterType }}/${{ parameters.name }}/catalog-info.yaml

    - id: completion-message
      name: Cluster Creation Complete
      action: debug:log
      input:
        message: |
          EKS Cluster Definition Completed!
          
          Pull Request: ${{ steps['publish-pr'].output.remoteUrl }}
          Cluster Path: ${{ parameters.environment }}/${{ parameters.clusterType }}/${{ parameters.name }}
          
          Next Steps:
          1. Verify the cluster appears in your Backstage catalog
          2. Check that the PR was merged successfully for Argo to sync
          3. Monitor Crossplane for cluster provisioning status

  output:
    links:
      - title: Pull Request
        url: ${{ steps['publish-pr'].output.remoteUrl }}
        icon: github
      - title: Repository
        url: https://github.com/SkylarHoughtonGithub/skylab-backstage-configs
    text:
      - title: Cluster Path
        content: ${{ parameters.environment }}/${{ parameters.clusterType }}/${{ parameters.name }}
      - title: Branch Name
        content: add-${{ parameters.environment }}-${{ parameters.clusterType }}-${{ parameters.name }}
      - title: Manual Registration URL
        content: https://github.com/SkylarHoughtonGithub/skylab-backstage-configs/blob/main/${{ parameters.environment }}/${{ parameters.clusterType }}/${{ parameters.name }}/catalog-info.yaml
